<?php
$viewModel = $block->getViewModel();


$categoryProducts = $viewModel->getProductCollection();

foreach ($categoryProducts as $product) {
    echo "<pre>";
    print_r($product->getData());
    echo "</pre>";
}
?>
